# ───────────────────────────────────────────────────────
# Lint rules — เข้มงวดเพื่อ production quality
# Reference: https://dart.dev/lints
# ───────────────────────────────────────────────────────

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # บังคับ
    invalid_annotation_target: ignore  # สำหรับ riverpod
    # เปลี่ยน warning เป็น error
    missing_required_param: error
    missing_return: error
    must_be_immutable: error
    # avoid_print เป็น error ใน production
    avoid_print: error
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated/**"
    - "lib/l10n/gen/**"
  plugins:
    - custom_lint

linter:
  rules:
    # Style & Best Practices
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_print: true
    avoid_returning_null_for_void: true
    avoid_unused_constructor_parameters: true
    cancel_subscriptions: true
    close_sinks: true
    deprecated_consistency: true
    # Async
    avoid_void_async: true
    discarded_futures: true
    unawaited_futures: true
    # Documentation
    package_api_docs: false  # ไม่บังคับสำหรับ private app
    public_member_api_docs: false
    # Type Safety
    avoid_types_as_parameter_names: true
    avoid_returning_this: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    use_super_parameters: true
    # Performance
    avoid_function_literals_in_foreach_calls: true
    prefer_collection_literals: true
    # Safety
    use_build_context_synchronously: true
